Imports System.IO
Imports System.Data
Imports System.Data.SqlClient
Imports System.Data.OleDb
Imports System.Data.Odbc

Imports System.Web
Imports System.Web.UI
Imports System.Web.UI.WebControls
Partial Public Class home_class
    Inherits stdpage
    Public CreditPercent
    Public CreditPercent2
    'Generated by VI SOFTLABS FORM GENERATOR (http://www.vifeandi.net) VER 2014.1
    Protected Sub Page_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load
        listingpage = ""
        _FormsName = ""
        TableName = ""
        IDPField = ""
        IDField = ""
        APPIDField = ""
        MerchantIDField = ""
        FilterField = ""


        If (weblib.CustNum & "").trim <> "" Then
            Dim obj As New RuntimeCustomer

            Call obj.getCreditInfo(Weblib.MerchantID)
            CreditPercent2 = obj.CreditPercent2
            CreditPercent = obj.CreditPercent
            obj = Nothing


        End If
        litbulb.text = "<img src=""" & "graphics/misc/offbulb.png" & """ />"
        litcredit.text = "You got no credit limit alert"

        Call initLoad()
        lblmessage.text = ""
        If Page.IsPostBack = False Then


            If isnumeric(CreditPercent) = True Then

                Dim lmessaget As String = "<br><b>Tolerance ±</b> on the percentage due to daily movement. Please refer to our sales representative for details."


                litcredit.text = "<font style=""font-size:50px;color:red"">" & CreditPercent & "%</font><br>You have reached <b>" & CreditPercent & "%</b> of your available credit limit" & lmessaget

                If CDbl(CreditPercent) > 60 Then
                    litbulb.text = "<img src=""" & "graphics/misc/redbulb.png" & """ />"
                End If
            End If


            If weblib.isstaff = True Then
                If (CreditPercent2 & "").trim <> "" Then
                    If (litcredit.text & "").trim <> "" Then
                        litcredit.text = litcredit.text & "<br><br>"
                    End If

                    litcredit.text = litcredit.text & "<br><b>Internal Info (Staff only) :</b><br><font style=""font-size:50px;color:red"">" & CreditPercent2 & "%</font><br>You have reached <b>" & CreditPercent2 & "%</b> of your available credit limit (including open orders)."
                End If
            End If
        End If
    End Sub

End Class

