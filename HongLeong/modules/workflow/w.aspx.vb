Imports System.IO
Imports System.Data
Imports System.Data.SqlClient
Imports System.Data.OleDb
Imports System.Web
Imports System.Web.UI
Imports System.Web.UI.WebControls
Partial Public Class w_class
    Inherits detailspage


    Private f_size As String
    'Generated by VI FEANDI SOFTLABS FORM GENERATOR (http://www.vifeandi.net) VER 2014.1
    Protected Sub Page_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load
        listingpage = "wlist.aspx"
        _FormsName = "Workflow Details"
        TableName = "Workflow"
        IDPField = ""
        IDField = "wf_id"
        AppIDField = ""
        MerchantIDField = ""
        FilterField = "wf_filter"
        APPCode = ""
        AddRights = "" '"SM0001"
        DelRights = "" '"SM0003"
        ModRights = "" '"SM0002"
        ViewRights = "" '"SM0004"
        FullRights = ""
        NmSpace = "workflow"

        Dim obj As New ViFeandi.General
        f_size = obj.GetPageSizeString(True, "")
        obj = Nothing

        If Page.IsPostBack = False Then
            Call WebLib.SetListItems(wf_classification, WebLib.ClassificationString)
        End If
        Call InitLoad()
    End Sub
    Private Function ValidateForm()
        lblmessage.Text = ""
        If wf_code.Text.Trim = "" Then
            lblMessage.Text = WebLib.getAlertMessageStyle("Workflow Code is Mandatory")
            Return False
            Exit Function
        End If

        If wf_name.Text.Trim = "" Then
            lblMessage.Text = WebLib.getAlertMessageStyle("Workflow Name is Mandatory")
            Return False
            Exit Function
        End If
        If wf_classification.SelectedIndex < 0 Then
            lblMessage.Text = WebLib.getAlertMessageStyle("Classification is Mandatory")
            Return False
            Exit Function
        End If

        If WebLib.CodeExists(wf_code.Text, "wf_code", TableName, IDField, rid.Value, IDPField, bid.Value, AppIDField, MerchantIDField, FilterField) = True Then
            lblMessage.Text = "This Workflow Code Already Exits"
            Return False
            Exit Function
        End If



        Return True
    End Function
    Public Overrides Function LoadData() As Boolean
        Dim cn As New OleDbConnection(connectionstring)
        Dim cmd As New OleDbCommand()
        Dim ad As New OleDb.OleDbDataAdapter()
        Dim ds As New DataSet()
        Dim counter As Integer = 0
        Dim dr As DataRow
        Dim templi As ListItem
        If IsNumeric(rid.Value) = False Then
            Exit Function
        End If

        Try
            cmd.CommandText = "Select * from workflow where wf_id=" & rid.Value
            cmd.Connection = cn
            ad.SelectCommand = cmd
            ad.Fill(ds, "datarecords")
            For Each dr In ds.Tables("datarecords").Rows

                counter = counter + 1

                wf_code.Text = dr("wf_code") & ""
                wf_name.Text = dr("wf_name") & ""
                templi = wf_classification.Items.FindByValue(dr("wf_classification") & "")
                wf_classification.SelectedIndex = wf_classification.Items.IndexOf(templi)

                Exit For
            Next
            cn.Close()
            cmd.Dispose()
            cn.Dispose()
        Catch ex As Exception
            lblmessage.Text = WebLib.getAlertMessageStyle(ex.Message)
        End Try
    End Function
    Public Sub savepage(ByVal sender As System.Object, ByVal e As System.EventArgs)
        Dim insertfields, insertvalues As String
        Dim lsrdesc As String = ""
        insertfields = ""
        insertvalues = ""
        If ValidateForm() = False Then
            Exit Sub
        End If
        Dim lwf_class As String = ""
        If wf_classification.SelectedIndex < 0 Then
            lwf_class = ""
        Else
            lwf_class = wf_classification.SelectedItem.Value
        End If

        Try
            If rid.Value = "" Then
                insertfields = insertfields & "wf_merchantid"
                insertvalues = insertvalues & "'" & WebLib.MerchantID.replace("'", "''") & "'"
                insertfields = insertfields & ",wf_filter"
                insertvalues = insertvalues & ",'" & WebLib.FilterCode.replace("'", "''") & "'"
                insertfields = insertfields & ",wf_code"
                insertvalues = insertvalues & ",'" & wf_code.Text.Replace("'", "''") & "'"
                insertfields = insertfields & ",wf_name"
                insertvalues = insertvalues & ",'" & wf_name.Text.Replace("'", "''") & "'"
                insertfields = insertfields & ",wf_classification"
                insertvalues = insertvalues & ",'" & lwf_class.Replace("'", "''") & "'"
                insertfields = insertfields & ",wf_createby"
                insertvalues = insertvalues & ",'" & WebLib.LoginUser.replace("'", "''") & "'"
                insertfields = insertfields & ",wf_createdt"
                insertvalues = insertvalues & ",getdate()"
                insertfields = insertfields & ",wf_updateby"
                insertvalues = insertvalues & ",'" & WebLib.LoginUser.replace("'", "''") & "'"
                insertfields = insertfields & ",wf_updatedt"
                insertvalues = insertvalues & ",getdate()"

            Else
                insertvalues = insertvalues & "wf_name='" & wf_name.Text.Replace("'", "''") & "'"
                insertvalues = insertvalues & ",wf_code='" & wf_code.Text.Replace("'", "''") & "'"
                insertvalues = insertvalues & ",wf_classification='" & lwf_class.Replace("'", "''") & "'"
                insertvalues = insertvalues & ",wf_updateby='" & WebLib.LoginUser.replace("'", "''") & "'"
                insertvalues = insertvalues & ",wf_updatedt=getdate()"

            End If

            If savedata(insertfields, insertvalues) = True Then
                Call gotoback()
            End If
        Catch Err As Exception
            lblMessage.Text = WebLib.getAlertMessageStyle(Err.Message)
        Finally

        End Try
    End Sub
End Class

